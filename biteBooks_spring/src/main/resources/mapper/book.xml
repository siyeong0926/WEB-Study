<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gudi.biteBooks.bookMapper">
	<select id="bookList" parameterType="java.util.HashMap" resultType="map">
		SELECT
		b_no,b_title,b_author,b_publisher, b_descript, b_genre, b_type
		FROM book
		<where>
			<if test="b_no!=null">AND b_no=#{b_no}</if>
			<if test="gubun!=null">
				<choose>
					<when test='gubun.equals("n_title")'>
						AND (b_title LIKE '%'||#{keyword}||'%')
					</when>
					<when test='gubun.equals("n_writer")'>
						AND (b_author LIKE '%'||#{keyword}||'%')
					</when>
					<when test='gubun.equals("n_content")'>
						AND (b_descript LIKE '%'||#{keyword}||'%')
					</when>
				</choose>
			</if>
		</where>
		ORDER BY b_no desc
	</select>	 		
</mapper>
